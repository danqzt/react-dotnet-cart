to run the application.
1. With Docker, run command from main folder:
   - docker-compose up
   - browse to http://localhost:3000

2. Without docker in vscode
   Open terminal go to 
     - server/src folder -> then run command: dotnet run
     - ui folder -> then run : 
            npm install 
            npm start
     - browse to http://localhost:3000

3. Run unit test.
   Open terminal go to:
     - server/test folder -> then run command: dotnet test